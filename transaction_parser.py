import re\n\nclass TransactionParser:\n    def __init__(self, email_body):\n        self.email_body = email_body\n\n    def parse(self):\n        amounts = self.extract_amounts()\n        merchants = self.extract_merchants()\n        transactions = list(zip(merchants, amounts))\n        return transactions\n\n    def extract_amounts(self):\n        # Assuming amounts are prefixed by the currency symbol\n        pattern = r'\$[\d,]+(?:\.\d{2})?'  # Modify according to the currency symbol as necessary\n        return re.findall(pattern, self.email_body)\n\n    def extract_merchants(self):\n        # Simplified example to extract merchant names, more complex regex may be needed\n        # Assuming merchant names follow the amount\n        pattern = r'(?<=\$[\d,]+(?:\.\d{2})?\s)([\w\s]+)'\n        return re.findall(pattern, self.email_body)\n\n# Example usage (uncomment to use):\n# email = "Your transaction of $123.45 at Starbucks was successful."\n# parser = TransactionParser(email)\n# print(parser.parse())\n